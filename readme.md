# AWS IoT Thing Creator

A script to easily create AWS IoT Things with certificates.

## Usage

Create a thing with attributes

```
create_thing.sh [type] [location] [model]
```

For example:

```
create_thing.sh camera office v2
```
